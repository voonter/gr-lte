# auto-generated by grc.converter

id: lte_pbch_demux_vcvc
label: PBCH Demux
category: lte

parameters:
-   id: rxant
    label: RX antennas
    dtype: int
    default: '1'
-   id: N_rb_dl
    label: resource blocks
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 12 * N_rb_dl * rxant }
-   domain: message
    id: cell_id
    optional: true

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ 240 * rxant }

templates:
    imports: from gnuradio import lte
    make: lte.pbch_demux_vcvc(${N_rb_dl}, ${rxant}, "$id")

file_format: 1
