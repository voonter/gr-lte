# auto-generated by grc.converter

id: lte_sss_symbol_selector_cvc
label: SSS Symbol Selector
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftl }

templates:
    imports: from gnuradio import lte
    make: lte.sss_symbol_selector_cvc(${fftl}, "$id")

file_format: 1
