# auto-generated by grc.converter

id: lte_layer_demapper_vcvc
label: Layer demapper
category: lte

parameters:
-   id: N_ant
    label: antenna ports
    dtype: int
-   id: vlen
    label: vector length
    dtype: int
-   id: style
    label: RX style
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   domain: message
    id: N_ant
    optional: true

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: from gnuradio import lte
    make: lte.layer_demapper_vcvc(${N_ant}, ${vlen}, ${style}, "$id")

file_format: 1
