# auto-generated by grc.converter

id: lte_mimo_sss_symbol_selector
label: MIMO SSS symbol selector
category: lte

parameters:
-   id: rxant
    label: RX antennas
    dtype: int
-   id: N_rb_dl
    label: resource blocks
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ rxant*N_rb_dl*12 }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ rxant*72 }

templates:
    imports: from gnuradio import lte
    make: lte.mimo_sss_symbol_selector(${rxant}, ${N_rb_dl})

file_format: 1
