# auto-generated by grc.converter

id: lte_mimo_sss_tagger
label: MIMO SSS tagger
category: lte

parameters:
-   id: rxant
    label: RX antennas
    dtype: int
-   id: N_rb_dl
    label: resource blocks
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ rxant*N_rb_dl*12 }
-   domain: message
    id: frame_start

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ rxant*N_rb_dl*12 }

templates:
    imports: from gnuradio import lte
    make: lte.mimo_sss_tagger(${rxant}, ${N_rb_dl})

file_format: 1
