# auto-generated by grc.converter

id: lte_sync_frequency_c
label: CP FFO Calculator
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: sig
    label: Signal Source
    dtype: raw

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: message
    id: freq

templates:
    imports: from gnuradio import lte
    make: lte.sync_frequency_c(${fftl}, "$id")

file_format: 1
