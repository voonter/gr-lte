# auto-generated by grc.converter

id: lte_crc_check_vbvb
label: Check CRC16
category: lte

parameters:
-   id: data_len
    label: payload length
    dtype: int
-   id: final_xor
    label: final XOR
    dtype: int

inputs:
-   domain: stream
    dtype: byte
    vlen: ${ data_len+16 }

outputs:
-   label: data
    domain: stream
    dtype: byte
    vlen: ${ data_len }
-   label: check
    domain: stream
    dtype: byte

templates:
    imports: from gnuradio import lte
    make: lte.crc_check_vbvb(${data_len}, ${final_xor}, "$id")

file_format: 1
