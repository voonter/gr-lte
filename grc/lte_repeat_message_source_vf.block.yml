# auto-generated by grc.converter

id: lte_repeat_message_source_vf
label: Repeat Message Source
category: lte

parameters:
-   id: vector_len
    label: vector length
    dtype: int

inputs:
-   domain: message
    id: vector

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vector_len }

templates:
    imports: from gnuradio import lte
    make: lte.repeat_message_source_vf(${vector_len}, "$id")

file_format: 1
