# auto-generated by grc.converter

id: lte_subblock_deinterleaver_vfvf
label: Subblock Deinterleaver
category: lte

parameters:
-   id: num_groups
    label: REGs per group
    dtype: int
-   id: items_per_group
    label: items per REG
    dtype: int

inputs:
-   domain: stream
    dtype: float
    vlen: ${ num_groups*items_per_group }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ num_groups*items_per_group }

templates:
    imports: from gnuradio import lte
    make: lte.subblock_deinterleaver_vfvf(${num_groups}, ${items_per_group}, "$id")

file_format: 1
