# auto-generated by grc.converter

id: lte_extract_subcarriers_vcvc
label: Extract subcarriers
category: lte

parameters:
-   id: N_rb_dl
    label: resource blocks
    dtype: int
-   id: fftl
    label: FFT length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftl }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ 12*N_rb_dl }

templates:
    imports: from gnuradio import lte
    make: lte.extract_subcarriers_vcvc(${N_rb_dl}, ${fftl}, "$id")

file_format: 1
