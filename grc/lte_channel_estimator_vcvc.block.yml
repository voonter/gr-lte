# auto-generated by grc.converter

id: lte_channel_estimator_vcvc
label: Channel estimator generic
category: lte

parameters:
-   id: rxant
    label: RX antennas
    dtype: int
    default: '1'
-   id: subcarriers
    label: subcarriers
    dtype: int
-   id: tag_key
    label: tag key value
    dtype: string
-   id: pilot_carriers
    label: pilot carrier map
    dtype: raw
-   id: pilot_symbols
    label: pilot symbol values
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers * rxant }
-   domain: message
    id: pilots
    optional: true

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ subcarriers * rxant }

templates:
    imports: from gnuradio import lte
    make: lte.channel_estimator_vcvc(${rxant}, ${subcarriers}, ${tag_key}, "pilots",
        ${pilot_carriers}, ${pilot_symbols}, "$id")

file_format: 1
